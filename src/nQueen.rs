use std::env;


fn nqueen(nq: i32, coor: [i32; 30], n: i8) {
    if nq == n {
        ret += 1;
    }
}

pub fn run() {
    let args_: Vec<String> = env::args().collect();
    let n = args_[1].clone();
    //n.into();
    let mut ret: i32 = 0;
}